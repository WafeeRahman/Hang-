<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit SpotGround</title>
</head>

<body>
    <h1>Edit SpotGround</h1>
    <form action="/spotgrounds/<%=spot._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="spotgrounds[title]" value="<%= spot.title %>">
        </div>

        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="spotgrounds[location]" value="<%= spot.location %>">
        </div>

        <button>Update Spot</button>

    </form>

    <footer>
        <a href="/spotgrounds/<%=spot._id%>">Cancel</a>
    </footer>

</body>

</html>